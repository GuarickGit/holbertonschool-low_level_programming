#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	/*Utilisation de la commande Puts*/
	puts("\"Programming is like building a multilingual puzzle");

	return (0);
}
